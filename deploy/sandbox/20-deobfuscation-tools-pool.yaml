apiVersion: extensions.agents.x-k8s.io/v1beta1
kind: SandboxWarmPool
metadata:
  name: deobfuscation-tools-pool
  namespace: agent-sandbox-demo
spec:
  replicas: 1
  sandboxTemplateRef:
    name: deobfuscation-tools-runtime-template
